name: Workflow - Push

on:
  push:
    branches:
      - main

jobs:
  push_job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Mensaje de Push
        run: echo "Workflow activado por push"

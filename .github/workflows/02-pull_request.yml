name: Workflow - Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  pull_request_job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Mensaje de Pull Request
        run: echo "Pull request detectado"

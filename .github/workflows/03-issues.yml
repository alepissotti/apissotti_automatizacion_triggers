name: Workflow - Issues

on:
  issues:
    types:
      - opened

jobs:
  issues_job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Mensaje de Nuevo Issue
        run: echo "Nuevo issue creado"

name: Workflow - Issue Comment

on:
  issue_comment:
    types:
      - created

jobs:
  issue_comment_job:
    runs-on: ubuntu-latest
    # Solo ejecutar si el comentario es en un Pull Request
    if: github.event.issue.pull_request != null
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Mensaje de Comentario en PR
        run: echo "Comentario en PR detectado"
